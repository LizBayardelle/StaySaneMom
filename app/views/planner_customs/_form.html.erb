</div> <!-- container -->



  <%= simple_form_for(@planner_custom) do |f| %>
    <%= f.error_notification %>
    <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

      <% if @planner_custom.purchase == nil %>
        <%= f.hidden_field :purchase, value: Purchase.where(user_id: current_user.id).last %>
      <% end %>
      <% if @planner_custom.user == nil %>
        <%= f.hidden_field :user, value: current_user %>
      <% end %>
      <%= f.hidden_field :date_ordered, value: Date.today %>

      <% if current_user.admin %>
        <div class="half-buffer"></div>
        <%= render "layouts/quadborder" %>
        <div class="half-buffer"></div>

        <div class="container">
        <h2>Admin Area</h2>
        <%= f.input :status, value: "Design Submitted" %>
        </div> <!-- container -->
      <% end %>


<div class="half-buffer"></div>
<%= render "layouts/quadborder" %>
<div class="half-buffer"></div>

<div class="container">

    <h2>Period of Time Covered</h2>
    <div class="row">
      <div class="form-group col-sm-6">
        <%= f.label "Starting Day of Planner" %>
        <input placeholder="Select First Day of Planner" type="text" id="date" class="form-control datepicker"></input>
        <%= f.hidden_field :date_starting, id: "dateHolder", class: "hiddenDateField" %>
      </div> <!-- form group -->

      <div class="form-group col-sm-6">
        <%= f.input :time_period, input_html: {class: 'browser-default'}, collection: [['One Month', 'One Month'], ['Two Months', 'Two Months'], ['Quarter', 'Quarter'], ['6 Months', '6 Months'], ['Other', 'Other']] %>
      </div> <!-- form group -->
    </div> <!-- row -->

  </div> <!-- container -->

  <div class="half-buffer"></div>
  <%= render "layouts/quadborder" %>
  <div class="half-buffer"></div>

  <div class="container">

    <h2>Calendar Spreads</h2>
    <div class="form-group">
      <%= f.input :monthly, label: "Monthly Overview Pages" %>
    </div>
    <div class="form-group padded-form-text">
      <%= f.input :monthly_comments, label: "What to Include on the Monthly Overview Pages" %>
    </div> <!-- form-group -->
    <div class="form-group">
      <%= f.input :weekly, label: "Weekly Overview Pages" %>
    </div> <!-- form-group -->
    <div class="form-group padded-form-text">
      <%= f.input :weekly_comments, label: "What to Include on the Weekly Overview Pages" %>
    </div> <!-- form-group -->
    <div class="form-group">
      <%= f.input :daily, label: "Daily Drilldown Pages" %>
    </div> <!-- form-group -->
    <div class="form-group padded-form-text">
      <%= f.input :daily_comments, label: "What to Include on the Daily Drilldown Pages" %>
    </div> <!-- form-group -->

    <div class="form-group">
      <%= f.label "Any other comments about your calendar spreads?" %>
      <%= f.text_area :planner_increment, class: 'form-control', class: "form-control" %>
    </div>

  </div> <!-- container -->

  <div class="half-buffer"></div>
  <%= render "layouts/quadborder" %>
  <div class="half-buffer"></div>

  <div class="container">

    <h2>Content Questions</h2>
    <div class="form-group">
      <%= f.label "In what increments of time do you like your schedule?" %>
      <%= f.text_area :schedule, placeholder: "e.g. 15 minute increments, 1-hour increments, unlined boxes for morning/afternoon/evening, etc.", class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label "What other spreads or pages do you want in your planner?" %>
      <%= f.text_area :segments, placeholder: "e.g. birthday/anniversary tracker, expenses report, debt snowball tracker, password keeper, project planner, goal setting, to do list, brainstorming, information pages, etc.", class: "form-control" %>
    </div>

  </div> <!-- container -->

  <div class="half-buffer"></div>
  <%= render "layouts/quadborder" %>
  <div class="half-buffer"></div>

  <div class="container">

    <h2>Design Questions</h2>
    <div class="form-group">
      <%= f.label "What three adjectives do you want your planner to embody?" %>
      <%= f.text_area :adjectives, placeholder: "e.g. Fun, girly, and flowery; bold, geometric, and colorful; professional, minimalist, and witty", class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label "Are there any specific colors you want included (or avoided)?" %>
      <%= f.text_area :colors, placeholder: "If you have specific hex codes (for your business or just preferences) definitely include them here.", class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label "Are there any specific fonts you want included (or avoided)?" %>
      <%= f.text_area :fonts, placeholder: "Feel free to name specific fonts, or just say something like 'please no cursive', 'I hate serif fonts', or 'I love handwritten fonts'", class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label "Are there any other themes or designs you want included?" %>
      <%= f.text_area :themes, placeholder: "You can request something with flowers, polka dots, splashing paint droplets, the sky is the limit!  Make it yours!", class: "form-control" %>
    </div>

  </div> <!-- container -->

  <div class="half-buffer"></div>
  <%= render "layouts/quadborder" %>
  <div class="half-buffer"></div>

  <div class="container">

    <h2>Personalization Questions</h2>
    <div class="form-group">
      <%= f.input :logo, label: "I have a specific logo (or logs) I want used." %>
    </div> <!-- form-group -->
    <div class="form-group">
      <%= f.label "Upload Logo(s)" %><br />
      <%= f.file_field :logos, multiple: true %>
    </div>
    <div class="form-group">
      <%= f.label "How would you like your name, brand, or other personalization?" %>
      <%= f.text_area :personalization, placeholder: "e.g. First name only ('Kate'), initials ('KJB'), brand name ('The Color Coded Mom'), etc.", class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label "Do you have a specific tagline or quote you want used?" %>
      <%= f.text_area :tagline, placeholder: "Business taglines are great, short quotes also work well.", class: "form-control" %>
    </div>

  </div> <!-- container -->

  <div class="half-buffer"></div>
  <%= render "layouts/quadborder" %>
  <div class="half-buffer"></div>

  <div class="container">

    <h2>Other Information</h2>
    <div class="form-group">
      <%= f.label "Anything else you want for your planner?  The more detailed you are, the better it will fit your needs!" %>
      <%= f.text_area :other_comments, class: "form-control" %>
    </div>

  </div> <!-- container -->

  <div class="half-buffer"></div>
  <%= render "layouts/quadborder" %>
  <div class="half-buffer"></div>

  <div class="container">

    <h2>Submit Design</h2>
    <p class="text-center">Only check this box when you're ready to submit.<br />You won't be able to make changes after the design is submitted.</p>
    <%= f.input :submitted, label: "This is my final design." %>
  </div>

  <div class="form-actions text-center">
    <%= f.submit "Save My Design", class: "btn btn-danger" %>
  </div>
<% end %>

</div> <!-- container -->

<div class="half-buffer"></div>
<%= render "layouts/quadborder" %>
<div class="half-buffer"></div>

<div class="container">



<%= content_for :additional_js_partial do %>
  <script type="text/javascript">
    $(document).ready(function() {
      $('.datepicker').pickadate();
      $('#date').on('change', function() {
        var date = $('#date').val();
        $('#dateHolder').val(date);
      });
    });
  </script>
<% end %>
